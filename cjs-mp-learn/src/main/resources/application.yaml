spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mp2020?useSSL=false&serverTimezone=UTC
    username: root
    password: 123456
logging:
  level:
    root: warn
    com.springcloud.mp.dao: trace
  pattern:
    console: '%p%m%n'
